commit 930a8a7dd4708666c30a940943e7e6e0baf47a91
Author: Claude <noreply@anthropic.com>
Date:   Sat May 30 18:46:23 2026 +0000

    Fix: actually return third_party_state from invoice-states endpoint
    
    The previous commit's controller edit silently didn't apply, so the endpoint
    never returned third_party_state while its test asserted it — the branch tip
    was red. Add the field for real; states test + full suite green (502).
    
    https://claude.ai/code/session_011uTDhCkQBYxxAXj7iDDHqs

diff --git a/app/Http/Controllers/Webhooks/WhmcsInvoiceStatesController.php b/app/Http/Controllers/Webhooks/WhmcsInvoiceStatesController.php
index 64fc1a6..bdeb29c 100644
--- a/app/Http/Controllers/Webhooks/WhmcsInvoiceStatesController.php
+++ b/app/Http/Controllers/Webhooks/WhmcsInvoiceStatesController.php
@@ -104,6 +104,9 @@ public function __invoke(Request $request, string $slug): JsonResponse
                 // Prefer the invoice's MARK (authoritative once issued via the
                 // lifecycle); fall back to the pending row's cached mark.
                 'mydata_mark' => $row->invoice?->mydata_mark ?? $row->mydata_mark,
+                // Third-party routing bucket (none/single/multi/null) so the
+                // addon list can flag «παραστατικό σε τρίτο» without a 2nd call.
+                'third_party_state' => $row->third_party_state,
             ];
         }
 
