{{-- ekdosi: the "© <year> <app name>. All rights reserved." footer is intentionally
     dropped from customer-facing mail — the same app-name («ekdosi») leak as the
     header. The tenant's own contact block (phone / email / ΑΦΜ) is carried in each
     message's subcopy, which is the footer that actually belongs on the email. --}}
